name: grocery_store_native
description: Customer-facing grocery store app (Flutter).
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  path_provider: ^2.1.3

flutter:
  uses-material-design: true
